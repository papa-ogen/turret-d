/* globals __DEV__ */
import Phaser from 'phaser'

import Enemy from '../sprites/Enemy'

export default class extends Phaser.Scene {
  constructor () {
    super('GameScene')
  }
  init () {}
  preload () {}

  create () {
  }

  update (time, delta) {
  }
}
